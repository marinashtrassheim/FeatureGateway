# pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.24.0
# Starlette TestClient (FastAPI 0.104) передаёт app= в httpx.Client; в httpx 0.28+ это убрано.
httpx>=0.27.0,<0.28
